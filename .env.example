Exit code: 0
Wall time: 1.2 seconds
Output:
# Safe local-development placeholders. Never put production secrets in this file.
RESUMEGRAPH_ENVIRONMENT=development
RESUMEGRAPH_LOG_LEVEL=INFO
RESUMEGRAPH_DATABASE_URL=postgresql+asyncpg://resumegraph:resumegraph-local-only@127.0.0.1:5432/resumegraph
RESUMEGRAPH_REDIS_URL=redis://127.0.0.1:6379/0
RESUMEGRAPH_READINESS_TIMEOUT_SECONDS=1.0
RESUMEGRAPH_DEPENDENCY_TIMEOUT_SECONDS=3.0
RESUMEGRAPH_ADMIN_SESSION_COOKIE_NAME=resumegraph_admin_session
RESUMEGRAPH_ADMIN_SESSION_TTL_SECONDS=28800
# Local HTTP development only. Production startup requires this to be true and HTTPS enabled.
RESUMEGRAPH_COOKIE_SECURE=false
RESUMEGRAPH_ADMIN_LOGIN_MAX_FAILURES=5
RESUMEGRAPH_ADMIN_LOGIN_WINDOW_SECONDS=300
# Safe local placeholder only. Generate and inject a separate secret in each deployment.
RESUMEGRAPH_ACCESS_TOKEN_PEPPER=local-development-access-token-pepper-change-me
RESUMEGRAPH_RECRUITER_SESSION_COOKIE_NAME=resumegraph_recruiter_session
RESUMEGRAPH_RECRUITER_SESSION_TTL_SECONDS=14400
RESUMEGRAPH_ACCESS_EXCHANGE_FAILURE_LIMIT=10
RESUMEGRAPH_ACCESS_EXCHANGE_FAILURE_WINDOW_SECONDS=600
RESUMEGRAPH_MARKDOWN_MAX_BYTES=1048576
# Paragraph-aware secondary split target. A single indivisible paragraph may exceed it.
RESUMEGRAPH_CHUNK_MAX_CHARACTERS=2000
# Empty placeholder only. Inject the real value through the process environment.
RESUMEGRAPH_DEEPSEEK_API_KEY=
RESUMEGRAPH_DEEPSEEK_BASE_URL=https://api.deepseek.com
RESUMEGRAPH_DEEPSEEK_QUALITY_MODEL=deepseek-v4-pro
RESUMEGRAPH_DEEPSEEK_QUALITY_THINKING_ENABLED=false
RESUMEGRAPH_QUALITY_JUDGE_TIMEOUT_SECONDS=45
RESUMEGRAPH_QUALITY_JUDGE_MAX_RETRIES=2
RESUMEGRAPH_QUALITY_JUDGE_BATCH_SIZE=5
RESUMEGRAPH_QUALITY_RULE_MIN_CHARACTERS=80
RESUMEGRAPH_QUALITY_RULE_MAX_CHARACTERS=6000
RESUMEGRAPH_QUALITY_RULE_ABNORMAL_CHARACTER_RATIO=0.1
# Phase 3 compatible single-turn RAG. The Chat provider reuses the DeepSeek settings above.
RESUMEGRAPH_RAG_TOP_K=6
RESUMEGRAPH_RAG_MAX_CONTEXT_CHARACTERS=12000
RESUMEGRAPH_RAG_ANSWER_TIMEOUT_SECONDS=45
# Strict model or Citation output may be regenerated at most once.
RESUMEGRAPH_RAG_ANSWER_OUTPUT_RETRIES=1

# Phase 4 bounded LangGraph multi-Agent workflow and ephemeral Redis conversations.
RESUMEGRAPH_AGENT_SUPERVISOR_MAX_SPECIALIST_CALLS=4
RESUMEGRAPH_AGENT_PROFILE_MAX_TOOL_CALLS=2
RESUMEGRAPH_AGENT_PROJECT_MAX_TOOL_CALLS=2
RESUMEGRAPH_AGENT_TECHNICAL_MAX_TOOL_CALLS=2
RESUMEGRAPH_AGENT_VERIFICATION_MAX_RUNS=2
RESUMEGRAPH_AGENT_MAX_ANSWER_REPAIRS=1
RESUMEGRAPH_AGENT_MAX_GRAPH_STEPS=12
RESUMEGRAPH_AGENT_RUN_TIMEOUT_SECONDS=90
RESUMEGRAPH_CONVERSATION_MAX_TURNS=8
RESUMEGRAPH_CONVERSATION_TTL_SECONDS=3600
RESUMEGRAPH_CONVERSATION_SUMMARY_MAX_CHARACTERS=2000

# OpenAI-compatible Embedding API. Keep the API key empty in Git.
EMBEDDING_PROVIDER_NAME=zhipu
EMBEDDING_BASE_URL=https://open.bigmodel.cn/api/paas/v4
EMBEDDING_API_KEY=
EMBEDDING_MODEL=embedding-3
EMBEDDING_DIMENSIONS=1024
EMBEDDING_SEND_DIMENSIONS=true
EMBEDDING_BATCH_SIZE=10
EMBEDDING_TIMEOUT_SECONDS=30
EMBEDDING_MAX_RETRIES=2

# Docker Compose development values.
POSTGRES_DB=resumegraph
POSTGRES_USER=resumegraph
POSTGRES_PASSWORD=resumegraph-local-only
POSTGRES_PORT=5432
REDIS_PORT=6379
BACKEND_PORT=8000
