# Phase 2.4 最小知识索引前置实现计划

**目标：** 完成最小 Chunk 判断、统一索引 Job、pgvector 与 Fake Embedding 调用边界，并在真实 Embedding 模型选择前暂停。

**执行约束：** TDD；不 Commit/Tag/Push/Merge；不实现 RAG/LangGraph；不调用真实 Embedding 服务。

### 任务 1：最小 Migration 与模型

**文件：** `app/models/document_chunk.py`、`app/models/chunk_embedding.py`、`app/models/document_version.py`、`app/models/knowledge_document.py`、`app/models/ingestion_job.py`、`alembic/versions/c8e4f1a7b2d9_create_phase_2_4_mvp.py`、模型/Migration 测试。

- [ ] 先写失败测试，锁定 Chunk 字段、Embedding 唯一键、发布 FK、状态和 Job 类型/阶段。
- [ ] 添加 `pgvector` 依赖并更新 lock。
- [ ] 实现最小模型和可逆 Migration。
- [ ] 运行目标测试确认通过。

### 任务 2：轻量 DeepSeek Schema 与 Provider

**文件：** `app/schemas/indexing.py`、`app/quality/prompt.py`、`app/infrastructure/deepseek_quality.py` 及测试。

- [ ] 先写合法 JSON、缺失/伪造/重复 ID、非法枚举/长度/额外字段测试。
- [ ] 先写 Thinking disabled、temperature 0、JSON Output、空/非法响应有限重试、超时、429/5xx 与 401/402 测试。
- [ ] 实现最小 Schema、Prompt、共享异步客户端和安全错误码。
- [ ] 运行目标测试确认通过。

### 任务 3：统一 knowledge_indexing Job

**文件：** `app/repositories/indexing.py`、`app/services/indexing.py`、`app/services/indexing_worker.py`、`app/infrastructure/job_queue.py`、`app/worker.py` 及测试。

- [ ] 先写状态、活动 Job 幂等、秘密不外发、PII 脱敏、第一次写 enabled、重跑保留 enabled 的测试。
- [ ] 实现规则、LLM、Embedding、保存四阶段及安全失败。
- [ ] 复用同一个 ARQ Queue，只新增任务函数。
- [ ] 运行目标和 Phase 2.3 回归测试。

### 任务 4：Embedding Protocol、Fake 与 pgvector 边界

**文件：** `app/infrastructure/embedding.py`、索引 Worker/Repository 及测试。

- [ ] 先写批大小、超时、有限重试、结果数量和维度不一致测试。
- [ ] 实现 Protocol、Fake、未配置生产 Provider 和维度校验。
- [ ] 验证只保存 enabled Chunk、model/dimensions/content_hash 一致。
- [ ] 运行 Ruff、完整 pytest、Alembic upgrade/current/check、Compose config 和现有前端四项检查。
- [ ] 暂停并报告真实 Embedding 候选服务、模型、维度、费用、隐私和批大小待用户选择。
